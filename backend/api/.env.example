# Connection for the least-privilege API role (created by backend/db/seed.sql).
# RLS applies to every query; identity is set per-transaction via
# app.current_user_id / app.user_role.
DATABASE_URL=postgres://univest_api:univest@127.0.0.1:5432/univest
PORT=3000
